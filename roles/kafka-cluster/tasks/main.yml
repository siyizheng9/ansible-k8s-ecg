---
# tasks file for kafka-cluster

- name: cp yml files to the target machine
  synchronize:
    src: "{{ item }}"
    dest: kubernetes/{{ item }}
  with_items:
      - 'zookeeper/'
      - 'kafka/'
      - 'connector/'
      - 'mongo/'
      - 'mqtt/'
